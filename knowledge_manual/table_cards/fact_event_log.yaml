table_name: fact_event_log
card_version: v0.1
card_type: table_card
domain:
  id: C
  name: 产品使用
business_meaning: '事件日志（埋点流水）。

  数据字典将该表归入“产品使用”域。

  数据字典行数说明为：~200 万（本数据集体量最大）。'
grain: '数据字典说明粒度：一行 = 一次用户行为事件。

  数据字典说明主键：`event_id`。'
primary_key:
- event_id
natural_key_candidate: []
profile:
  row_count: 2000000
  column_count: 6
  source_profile_file: outputs/profiles/table_profiles.json
  source_column_profile_file: outputs/profiles/column_profiles.json
  profile_status: validated
  validation_file: outputs/knowledge/validation/fact_event_log_validation.md
columns:
  event_id:
    type: varchar
    semantic_type: primary_key
    business_meaning: 事件 ID（`E` 前缀）
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 2000000
      distinct_rate: 1.0
      min: E000008945d11
      max: Effffffe8e062
      sample_values:
      - E000008945d11
      - E000012b3edbd
      - E000013ff48fc
      - E000015b9e0f5
      - E000015d49d22
      is_possible_primary_key: true
    notes: []
  user_id:
    type: varchar
    semantic_type: foreign_key
    business_meaning: 用户
    references: dim_user.user_id
    profile:
      null_rate: 0.0
      distinct_count: 40016
      distinct_rate: 0.02
      min: U000001
      max: U049999
      sample_values:
      - U000001
      - U000003
      - U000004
      - U000005
      - U000006
      is_possible_primary_key: false
    notes: []
  event_name:
    type: varchar
    semantic_type: dimension
    business_meaning: 事件名。枚举：page_view / button_click / doc_create / doc_edit / msg_send / msg_read / file_upload / file_download / search / share / comment / reaction / calendar_create / calendar_rsvp / video_join / video_leave / task_create / task_complete / approval_submit / approval_approve
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 20
      distinct_rate: 0.0
      min: approval_approve
      max: video_leave
      sample_values:
      - approval_approve
      - approval_submit
      - button_click
      - calendar_create
      - calendar_rsvp
      is_possible_primary_key: false
    notes: []
  event_time:
    type: timestamp
    semantic_type: event_time
    business_meaning: 事件发生时间
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 1937818
      distinct_rate: 0.9689
      min: '2025-04-01 00:00:53'
      max: '2026-03-31 07:59:35'
      sample_values:
      - '2025-04-01 00:00:53'
      - '2025-04-01 00:01:22'
      - '2025-04-01 00:01:42'
      - '2025-04-01 00:02:22'
      - '2025-04-01 00:03:09'
      is_possible_primary_key: false
    notes: []
  page:
    type: varchar
    semantic_type: dimension
    business_meaning: 发生页面
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 16
      distinct_rate: 0.0
      min: /approval/list
      max: /wiki/page
      sample_values:
      - /approval/list
      - /base/table
      - /calendar
      - /docs/editor
      - /drive/folder
      is_possible_primary_key: false
    notes: []
  properties_json:
    type: varchar
    semantic_type: json_text
    business_meaning: 事件属性 JSON，不同事件有不同字段。常见：`doc_type`（docx/sheet/bitable/slides）、`msg_type`（text/image/file/sticker）、`experiment_id`（EXP001~EXP050）
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 459
      distinct_rate: 0.0002
      min: '{"doc_type": "bitable", "experiment_id": "EXP001"}'
      max: '{}'
      sample_values:
      - '{"doc_type": "bitable", "experiment_id": "EXP001"}'
      - '{"doc_type": "bitable", "experiment_id": "EXP002"}'
      - '{"doc_type": "bitable", "experiment_id": "EXP003"}'
      - '{"doc_type": "bitable", "experiment_id": "EXP004"}'
      - '{"doc_type": "bitable", "experiment_id": "EXP005"}'
      is_possible_primary_key: false
    notes: []
join_keys:
- column: user_id
  references: dim_user.user_id
  relationship: many_to_one
  join_condition: fact_event_log.user_id = dim_user.user_id
  purpose: '数据字典说明中的主关联路径。

    当前正式卡可作为推荐 JOIN 路径使用。'
  risk_level: low
  validation_status: passed
  note: '字段说明写明 user_id 表示用户。

    当前正式卡已通过对应 validation 检查，使用时仍需遵守本卡 grain 与 known_traps 规则。'
derived_join_paths: {}
field_groups:
  id_columns:
  - event_id
  - user_id
  time_columns:
  - event_time
  metric_columns: []
  dimension_columns:
  - event_id
  - user_id
  - event_name
  - event_time
  - page
  - properties_json
  enum_columns:
  - event_name
  - page
  json_columns:
  - properties_json
  text_columns: []
recommended_use_cases:
- 基于数据字典理解 fact_event_log 的表级业务含义与字段语义
- 在运行 validation 脚本前做主键、粒度、时间口径与枚举口径假设梳理
- 按数据字典声明的枚举字段做结构分布分析
- 基于数据字典说明的时间字段做趋势或窗口分析
preferred_metrics:
  entity_count:
    expression: COUNT(DISTINCT event_id)
    meaning: 实体或记录数量
    applicable_grain: table_grain
    note: '按当前正式卡 grain 与 validation 结果解释统计口径。

      对于非单列主键表，应结合 grain 与 distinct 规则解释记录数。'
known_traps:
- trap: 把数据字典声明的主键直接当作已验证事实。
  consequence: 如果当前 DuckDB 数据与字典存在偏差，可能误判粒度或唯一性。
  prevention: 将“`event_id`”只视为数据字典假设，必须以后续 validation 脚本结果为准。
- trap: 将 COUNT(*) 与 COUNT(DISTINCT 实体键) 混用。
  consequence: 在一对多 JOIN 或非唯一粒度表中，可能把记录数误读成实体数。
  prevention: 先依据数据字典粒度说明和 validation 结果确认使用哪个计数口径。
aggregation_notes:
- 数据字典说明该表粒度为：一行 = 一次用户行为事件。聚合前必须先验证这一粒度在当前数据中是否成立。
- 当前所有聚合说明都仅基于数据字典，不代表已经通过验证。
validation_summary:
  validation_file: outputs/knowledge/validation/fact_event_log_validation.md
  profile_filled: true
  grain_validated: true
  dq_validated: true
  ready_for_agent_recommendation: true
  key_results:
    row_count: 2000000
    column_count: 6
    validation_checks_total: 16
    validation_checks_failed: 0
    validation_checks_warning: 0
    key_findings:
    - fact_event_log 行数为 2000000，字段数为 6。
    - 主键候选当前唯一。
    - user_id 当前全部可关联到 dim_user.user_id。
    - 主要 SQL patterns 已执行。
    warnings: []
  interpretation:
  - validation 脚本已通过，当前卡已升级为正式 table card。
  - 主键、粒度、外键覆盖率与主要 SQL pattern 以当前 validation report 为准。
  - fact_event_log 行数为 2000000，字段数为 6。
  - 主键候选当前唯一。
  - user_id 当前全部可关联到 dim_user.user_id。
  - 主要 SQL patterns 已执行。
validation_checks:
- basic_table_profile
- column_count
- primary_key_uniqueness
- duplicated_primary_key_examples
- main_foreign_key_coverage_checks
- enum_distribution_checks
- time_date_parse_checks
- numeric_range_checks
- json_parse_checks
- main_sql_patterns_execution_checks
sql_patterns:
  time_distribution: "SELECT\n event_time AS time_key,\n COUNT(*) AS row_count\nFROM fact_event_log\nGROUP BY event_time\nORDER BY event_time;"
  enum_distribution: "SELECT\n event_name,\n COUNT(DISTINCT event_id) AS entity_count\nFROM fact_event_log\nGROUP BY event_name\nORDER BY entity_count DESC, event_name;"
  join_coverage_sample: "SELECT\n COUNT(*) AS row_count\nFROM fact_event_log base\nLEFT JOIN dim_user ref\n ON base.user_id = ref.user_id;"
prompt_summary: 'fact_event_log 是 事件日志（埋点流水）。

  数据字典声明 grain 为：一行 = 一次用户行为事件。

  数据字典主键说明为：`event_id`。

  常用 JOIN 假设包括：user_id -> dim_user.user_id。'
manual_review:
  reviewer: null
  review_status: validated
  review_notes:
  - 当前卡已结合 validation report 完成正式化整理。
  - 主键、自然键、JOIN 覆盖率与字段口径以当前 validation report 和正式卡内容为准。
  - 当前卡已结合 validation report 升级为正式版本。
  checklist:
  - confirmed: true
    item: 数据字典声明主键为：`event_id`，已结合当前数据完成唯一性核对。
  - confirmed: true
    item: 数据字典声明粒度为：一行 = 一次用户行为事件，已结合当前数据完成 grain 核对。
  - confirmed: true
    item: 数据字典说明行数约为：~200 万（本数据集体量最大），已与当前 profile 和 DuckDB 数据核对。
  - confirmed: true
    item: 数据字典给出的 JOIN 假设：fact_event_log.user_id -> dim_user.user_id，已结合当前数据完成覆盖率与口径核对。
  - confirmed: true
    item: fact_event_log 行数为 2000000，字段数为 6。
  - confirmed: true
    item: 主键候选当前唯一。
  - confirmed: true
    item: user_id 当前全部可关联到 dim_user.user_id。
  - confirmed: true
    item: 主要 SQL patterns 已执行。
  required: false
  reviewed: true
  review_source: outputs/knowledge/validation/fact_event_log_validation.md
status:
  authoring_status: validated
  profile_filled: true
  grain_validated: true
  dq_validated: true
  ready_for_agent: true
