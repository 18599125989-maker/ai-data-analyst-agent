table_name: fact_page_view
card_version: v0.1
card_type: table_card
domain:
  id: C
  name: 产品使用
business_meaning: '页面浏览。

  数据字典将该表归入“产品使用”域。

  数据字典行数说明为：~180 万。'
grain: '数据字典说明粒度：一行 = 一次页面访问。

  数据字典说明主键：`pv_id`。'
primary_key:
- pv_id
natural_key_candidate: []
profile:
  row_count: 1801438
  column_count: 6
  source_profile_file: outputs/profiles/table_profiles.json
  source_column_profile_file: outputs/profiles/column_profiles.json
  profile_status: validated
  validation_file: outputs/knowledge/validation/fact_page_view_validation.md
columns:
  pv_id:
    type: varchar
    semantic_type: primary_key
    business_meaning: PV ID（`PV` 前缀）
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 1801438
      distinct_rate: 1.0
      min: PV00000b9a757e
      max: PVffffeed45f8a
      sample_values:
      - PV00000b9a757e
      - PV00000c0f3c3a
      - PV000018f68a81
      - PV000032e42151
      - PV00003ef50e69
      is_possible_primary_key: true
    notes: []
  session_id:
    type: varchar
    semantic_type: foreign_key
    business_meaning: 关联会话
    references: fact_session.session_id
    profile:
      null_rate: 0.0
      distinct_count: 400000
      distinct_rate: 0.222
      min: S00000ba4ffe9
      max: Sfffff6103e0a
      sample_values:
      - S00000ba4ffe9
      - S00009eea73c7
      - S0000c612be7a
      - S0000d1ed1a0d
      - S0000e79d6c20
      is_possible_primary_key: false
    notes: []
  user_id:
    type: varchar
    semantic_type: foreign_key
    business_meaning: 用户
    references: dim_user.user_id
    profile:
      null_rate: 0.0
      distinct_count: 29809
      distinct_rate: 0.0165
      min: U000001
      max: U049999
      sample_values:
      - U000001
      - U000007
      - U000008
      - U000011
      - U000013
      is_possible_primary_key: false
    notes: []
  page_path:
    type: varchar
    semantic_type: dimension
    business_meaning: 页面路径，如 `/docs/editor`
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 16
      distinct_rate: 0.0
      min: /approval/list
      max: /wiki/page
      sample_values:
      - /approval/list
      - /base/table
      - /calendar
      - /docs/editor
      - /drive/folder
      is_possible_primary_key: false
    notes: []
  referrer:
    type: varchar
    semantic_type: dimension
    business_meaning: '上一页路径（可为空）

      空值、空字符串或混合类型语义来自数据字典说明，仍需结合实际数据分布复核。'
    references: null
    profile:
      null_rate: 0.0589
      distinct_count: 16
      distinct_rate: 0.0
      min: /approval/list
      max: /wiki/page
      sample_values:
      - /approval/list
      - /base/table
      - /calendar
      - /docs/editor
      - /drive/folder
      is_possible_primary_key: false
    notes: []
  view_duration_ms:
    type: bigint
    semantic_type: metric
    business_meaning: 停留时长（毫秒）
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 59501
      distinct_rate: 0.033
      min: '500'
      max: '60000'
      sample_values:
      - '500'
      - '501'
      - '502'
      - '503'
      - '504'
      is_possible_primary_key: false
    notes: []
join_keys:
- column: session_id
  references: fact_session.session_id
  relationship: many_to_one
  join_condition: fact_page_view.session_id = fact_session.session_id
  purpose: '数据字典说明中的主关联路径。

    当前正式卡可作为推荐 JOIN 路径使用。'
  risk_level: low
  validation_status: passed
  note: '字段说明明确 session_id 关联会话。

    当前正式卡已通过对应 validation 检查，使用时仍需遵守本卡 grain 与 known_traps 规则。'
- column: user_id
  references: dim_user.user_id
  relationship: many_to_one
  join_condition: fact_page_view.user_id = dim_user.user_id
  purpose: '数据字典说明中的主关联路径。

    当前正式卡可作为推荐 JOIN 路径使用。'
  risk_level: low
  validation_status: passed
  note: '字段说明写明 user_id 表示用户。

    当前正式卡已通过对应 validation 检查，使用时仍需遵守本卡 grain 与 known_traps 规则。'
derived_join_paths:
  page_view_to_tenant:
    path:
    - fact_page_view.user_id = dim_user.user_id
    - dim_user.tenant_id = dim_tenant.tenant_id
    purpose: '数据字典 JOIN 参考显示可经用户维度补充企业画像。'
    risk_level: medium
    validation_status: passed
field_groups:
  id_columns:
  - pv_id
  - session_id
  - user_id
  time_columns: []
  metric_columns:
  - view_duration_ms
  dimension_columns:
  - pv_id
  - session_id
  - user_id
  - page_path
  - referrer
  enum_columns:
  - page_path
  - referrer
  json_columns: []
  text_columns: []
recommended_use_cases:
- 基于数据字典理解 fact_page_view 的表级业务含义与字段语义
- 在运行 validation 脚本前做主键、粒度、时间口径与枚举口径假设梳理
preferred_metrics:
  entity_count:
    expression: COUNT(DISTINCT pv_id)
    meaning: 实体或记录数量
    applicable_grain: table_grain
    note: '按当前正式卡 grain 与 validation 结果解释统计口径。

      对于非单列主键表，应结合 grain 与 distinct 规则解释记录数。'
  view_duration_ms_sum:
    expression: SUM(view_duration_ms)
    meaning: view_duration_ms 的SUM指标
    applicable_grain: table_grain
    note: 当前为自动草稿，需要人工确认该数值字段是否适合直接聚合。
known_traps:
- trap: 把数据字典声明的主键直接当作已验证事实。
  consequence: 如果当前 DuckDB 数据与字典存在偏差，可能误判粒度或唯一性。
  prevention: 将“`pv_id`”只视为数据字典假设，必须以后续 validation 脚本结果为准。
- trap: 将 COUNT(*) 与 COUNT(DISTINCT 实体键) 混用。
  consequence: 在一对多 JOIN 或非唯一粒度表中，可能把记录数误读成实体数。
  prevention: 先依据数据字典粒度说明和 validation 结果确认使用哪个计数口径。
aggregation_notes:
- 数据字典说明该表粒度为：一行 = 一次页面访问。聚合前必须先验证这一粒度在当前数据中是否成立。
- 当前所有聚合说明都仅基于数据字典，不代表已经通过验证。
validation_summary:
  validation_file: outputs/knowledge/validation/fact_page_view_validation.md
  profile_filled: true
  grain_validated: true
  dq_validated: true
  ready_for_agent_recommendation: true
  key_results:
    row_count: 1801438
    column_count: 6
    validation_checks_total: 16
    validation_checks_failed: 0
    validation_checks_warning: 0
    key_findings:
    - fact_page_view 行数为 1801438，字段数为 6。
    - 主键候选当前唯一。
    - session_id 当前全部可关联到 fact_session.session_id。
    - user_id 当前全部可关联到 dim_user.user_id。
    - 主要 SQL patterns 已执行。
    warnings: []
  interpretation:
  - validation 脚本已通过，当前卡已升级为正式 table card。
  - 主键、粒度、外键覆盖率与主要 SQL pattern 以当前 validation report 为准。
  - fact_page_view 行数为 1801438，字段数为 6。
  - 主键候选当前唯一。
  - session_id 当前全部可关联到 fact_session.session_id。
  - user_id 当前全部可关联到 dim_user.user_id。
  - 主要 SQL patterns 已执行。
validation_checks:
- basic_table_profile
- column_count
- primary_key_uniqueness
- duplicated_primary_key_examples
- main_foreign_key_coverage_checks
- enum_distribution_checks
- time_date_parse_checks
- numeric_range_checks
- json_parse_checks
- main_sql_patterns_execution_checks
sql_patterns:
  enum_distribution: "SELECT\n page_path,\n COUNT(DISTINCT pv_id) AS entity_count\nFROM fact_page_view\nGROUP BY page_path\nORDER BY entity_count DESC, page_path;"
  metric_by_enum: "SELECT\n page_path,\n ROUND(SUM(view_duration_ms), 2) AS total_view_duration_ms\nFROM fact_page_view\nGROUP BY page_path\nORDER BY total_view_duration_ms DESC, page_path;"
  join_coverage_sample: "SELECT\n COUNT(*) AS row_count\nFROM fact_page_view base\nLEFT JOIN fact_session ref\n ON base.session_id = ref.session_id;"
prompt_summary: 'fact_page_view 是 页面浏览。

  数据字典声明 grain 为：一行 = 一次页面访问。

  数据字典主键说明为：`pv_id`。

  常用 JOIN 假设包括：session_id -> fact_session.session_id；user_id -> dim_user.user_id。'
manual_review:
  reviewer: null
  review_status: validated
  review_notes:
  - 当前卡已结合 validation report 完成正式化整理。
  - 主键、自然键、JOIN 覆盖率与字段口径以当前 validation report 和正式卡内容为准。
  - 当前卡已结合 validation report 升级为正式版本。
  checklist:
  - confirmed: true
    item: 数据字典声明主键为：`pv_id`，已结合当前数据完成唯一性核对。
  - confirmed: true
    item: 数据字典声明粒度为：一行 = 一次页面访问，已结合当前数据完成 grain 核对。
  - confirmed: true
    item: 数据字典说明行数约为：~180 万，已与当前 profile 和 DuckDB 数据核对。
  - confirmed: true
    item: 数据字典给出的 JOIN 假设：fact_page_view.session_id -> fact_session.session_id，已结合当前数据完成覆盖率与口径核对。
  - confirmed: true
    item: 数据字典给出的 JOIN 假设：fact_page_view.user_id -> dim_user.user_id，已结合当前数据完成覆盖率与口径核对。
  - confirmed: true
    item: fact_page_view 行数为 1801438，字段数为 6。
  - confirmed: true
    item: 主键候选当前唯一。
  - confirmed: true
    item: session_id 当前全部可关联到 fact_session.session_id。
  - confirmed: true
    item: user_id 当前全部可关联到 dim_user.user_id。
  - confirmed: true
    item: 主要 SQL patterns 已执行。
  required: false
  reviewed: true
  review_source: outputs/knowledge/validation/fact_page_view_validation.md
status:
  authoring_status: validated
  profile_filled: true
  grain_validated: true
  dq_validated: true
  ready_for_agent: true
