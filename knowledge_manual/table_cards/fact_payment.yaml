table_name: fact_payment
card_version: v0.1
card_type: table_card
domain:
  id: E
  name: 计费与订阅
business_meaning: '付款流水。

  数据字典将该表归入“计费与订阅”域。

  数据字典行数说明为：~1.6k。'
grain: '数据字典说明粒度：一行 = 一次付款尝试。**一张发票可能对应多条付款记录**（尝试、部分支付、重试）。

  数据字典说明主键：`payment_id`。'
primary_key:
- payment_id
natural_key_candidate: []
profile:
  row_count: 1624
  column_count: 6
  source_profile_file: outputs/profiles/table_profiles.json
  source_column_profile_file: outputs/profiles/column_profiles.json
  profile_status: validated
  validation_file: outputs/knowledge/validation/fact_payment_validation.md
columns:
  payment_id:
    type: varchar
    semantic_type: primary_key
    business_meaning: 付款 ID（`PAY` 前缀）
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 1624
      distinct_rate: 1.0
      min: PAY004ec0740f72
      max: PAYffb9f317d407
      sample_values:
      - PAY004ec0740f72
      - PAY00527be5a1ae
      - PAY006d9a1bcdda
      - PAY00729a8c5c78
      - PAY0077e6f124f1
      is_possible_primary_key: true
    notes: []
  invoice_id:
    type: varchar
    semantic_type: foreign_key
    business_meaning: 关联发票
    references: fact_invoice.invoice_id
    profile:
      null_rate: 0.0
      distinct_count: 1248
      distinct_rate: 0.7685
      min: INV0015263020ea
      max: INVff8576941c17
      sample_values:
      - INV0015263020ea
      - INV00b27daab4d7
      - INV00bf93de68fc
      - INV00d9a8ad70c3
      - INV00dbaccfcd52
      is_possible_primary_key: false
    notes: []
  method:
    type: varchar
    semantic_type: dimension
    business_meaning: 支付方式。枚举：credit_card / bank_transfer / alipay / wechat_pay
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 4
      distinct_rate: 0.0025
      min: alipay
      max: wechat_pay
      sample_values:
      - alipay
      - bank_transfer
      - credit_card
      - wechat_pay
      is_possible_primary_key: false
    notes: []
  amount:
    type: double
    semantic_type: metric
    business_meaning: 本次付款金额（可能小于发票金额）
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 1624
      distinct_rate: 1.0
      min: '146.71'
      max: '115599.75'
      sample_values:
      - '146.71'
      - '157.13'
      - '182.11'
      - '193.39'
      - '195.41'
      is_possible_primary_key: true
    notes: []
  status:
    type: varchar
    semantic_type: dimension
    business_meaning: 枚举：success / failed / pending
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 3
      distinct_rate: 0.0018
      min: failed
      max: success
      sample_values:
      - failed
      - pending
      - success
      is_possible_primary_key: false
    notes: []
  paid_at:
    type: timestamp
    semantic_type: event_time
    business_meaning: '付款完成时间（失败/待处理为空）

      空值、空字符串或混合类型语义来自数据字典说明，仍需结合实际数据分布复核。'
    references: null
    profile:
      null_rate: 0.258
      distinct_count: 1205
      distinct_rate: 0.742
      min: '2025-04-03 04:39:06'
      max: '2026-04-01 03:32:12'
      sample_values:
      - '2025-04-03 04:39:06'
      - '2025-04-06 18:33:11'
      - '2025-04-08 07:12:23'
      - '2025-04-08 13:28:46'
      - '2025-04-10 20:11:43'
      is_possible_primary_key: false
    notes: []
join_keys:
- column: invoice_id
  references: fact_invoice.invoice_id
  relationship: many_to_one
  join_condition: fact_payment.invoice_id = fact_invoice.invoice_id
  purpose: '数据字典说明中的主关联路径。

    当前正式卡可作为推荐 JOIN 路径使用。'
  risk_level: low
  validation_status: passed
  note: '字段说明写明 invoice_id 表示关联发票。

    当前正式卡已通过对应 validation 检查，使用时仍需遵守本卡 grain 与 known_traps 规则。'
derived_join_paths: {}
field_groups:
  id_columns:
  - payment_id
  - invoice_id
  time_columns:
  - paid_at
  metric_columns:
  - amount
  dimension_columns:
  - payment_id
  - invoice_id
  - method
  - status
  - paid_at
  enum_columns:
  - method
  - status
  json_columns: []
  text_columns: []
recommended_use_cases:
- 基于数据字典理解 fact_payment 的表级业务含义与字段语义
- 在运行 validation 脚本前做主键、粒度、时间口径与枚举口径假设梳理
- 按数据字典声明的枚举字段做结构分布分析
- 基于数据字典说明的时间字段做趋势或窗口分析
- 按数据字典口径区分金额、收入、付款、发票或预算类指标
preferred_metrics:
  entity_count:
    expression: COUNT(DISTINCT payment_id)
    meaning: 实体或记录数量
    applicable_grain: table_grain
    note: '按当前正式卡 grain 与 validation 结果解释统计口径。

      对于非单列主键表，应结合 grain 与 distinct 规则解释记录数。'
  amount_sum:
    expression: SUM(amount)
    meaning: amount 的SUM指标
    applicable_grain: table_grain
    note: 当前为自动草稿，需要人工确认该数值字段是否适合直接聚合。
known_traps:
- trap: 把数据字典声明的主键直接当作已验证事实。
  consequence: 如果当前 DuckDB 数据与字典存在偏差，可能误判粒度或唯一性。
  prevention: 将“`payment_id`”只视为数据字典假设，必须以后续 validation 脚本结果为准。
- trap: 把付款金额与发票金额或实收收入当成完全相同口径。
  consequence: 可能忽略支付状态、时间点和账期差异。
  prevention: 按数据字典和业务问题分别处理 payment、invoice、actual_revenue。
- trap: 将 COUNT(*) 与 COUNT(DISTINCT 实体键) 混用。
  consequence: 在一对多 JOIN 或非唯一粒度表中，可能把记录数误读成实体数。
  prevention: 先依据数据字典粒度说明和 validation 结果确认使用哪个计数口径。
aggregation_notes:
- 数据字典说明该表粒度为：一行 = 一次付款尝试。**一张发票可能对应多条付款记录**（尝试、部分支付、重试）。聚合前必须先验证这一粒度在当前数据中是否成立。
- 当前所有聚合说明都仅基于数据字典，不代表已经通过验证。
validation_summary:
  validation_file: outputs/knowledge/validation/fact_payment_validation.md
  profile_filled: true
  grain_validated: true
  dq_validated: true
  ready_for_agent_recommendation: true
  key_results:
    row_count: 1624
    column_count: 6
    validation_checks_total: 16
    validation_checks_failed: 0
    validation_checks_warning: 0
    key_findings:
    - fact_payment 行数为 1624，字段数为 6。
    - 主键候选当前唯一。
    - invoice_id 当前全部可关联到 fact_invoice.invoice_id。
    - 主要 SQL patterns 已执行。
    warnings: []
  interpretation:
  - validation 脚本已通过，当前卡已升级为正式 table card。
  - 主键、粒度、外键覆盖率与主要 SQL pattern 以当前 validation report 为准。
  - fact_payment 行数为 1624，字段数为 6。
  - 主键候选当前唯一。
  - invoice_id 当前全部可关联到 fact_invoice.invoice_id。
  - 主要 SQL patterns 已执行。
validation_checks:
- basic_table_profile
- column_count
- primary_key_uniqueness
- duplicated_primary_key_examples
- main_foreign_key_coverage_checks
- enum_distribution_checks
- time_date_parse_checks
- numeric_range_checks
- json_parse_checks
- main_sql_patterns_execution_checks
sql_patterns:
  time_distribution: "SELECT\n paid_at AS time_key,\n COUNT(*) AS row_count\nFROM fact_payment\nGROUP BY paid_at\nORDER BY paid_at;"
  enum_distribution: "SELECT\n method,\n COUNT(DISTINCT payment_id) AS entity_count\nFROM fact_payment\nGROUP BY method\nORDER BY entity_count DESC, method;"
  metric_trend: "SELECT\n paid_at AS time_key,\n ROUND(SUM(amount), 2) AS total_amount\nFROM fact_payment\nGROUP BY paid_at\nORDER BY paid_at;"
  join_coverage_sample: "SELECT\n COUNT(*) AS row_count\nFROM fact_payment base\nLEFT JOIN fact_invoice ref\n ON base.invoice_id = ref.invoice_id;"
prompt_summary: 'fact_payment 是 付款流水。

  数据字典声明 grain 为：一行 = 一次付款尝试。**一张发票可能对应多条付款记录**（尝试、部分支付、重试）。

  数据字典主键说明为：`payment_id`。

  常用 JOIN 假设包括：invoice_id -> fact_invoice.invoice_id。'
manual_review:
  reviewer: null
  review_status: validated
  review_notes:
  - 当前卡已结合 validation report 完成正式化整理。
  - 主键、自然键、JOIN 覆盖率与字段口径以当前 validation report 和正式卡内容为准。
  - 当前卡已结合 validation report 升级为正式版本。
  checklist:
  - confirmed: true
    item: 数据字典声明主键为：`payment_id`，已结合当前数据完成唯一性核对。
  - confirmed: true
    item: 数据字典声明粒度为：一行 = 一次付款尝试。**一张发票可能对应多条付款记录**（尝试、部分支付、重试），已结合当前数据完成 grain 核对。
  - confirmed: true
    item: 数据字典说明行数约为：~1.6k，已与当前 profile 和 DuckDB 数据核对。
  - confirmed: true
    item: 数据字典给出的 JOIN 假设：fact_payment.invoice_id -> fact_invoice.invoice_id，已结合当前数据完成覆盖率与口径核对。
  - confirmed: true
    item: fact_payment 行数为 1624，字段数为 6。
  - confirmed: true
    item: 主键候选当前唯一。
  - confirmed: true
    item: invoice_id 当前全部可关联到 fact_invoice.invoice_id。
  - confirmed: true
    item: 主要 SQL patterns 已执行。
  required: false
  reviewed: true
  review_source: outputs/knowledge/validation/fact_payment_validation.md
status:
  authoring_status: validated
  profile_filled: true
  grain_validated: true
  dq_validated: true
  ready_for_agent: true
