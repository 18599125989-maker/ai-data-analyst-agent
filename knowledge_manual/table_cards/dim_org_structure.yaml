table_name: dim_org_structure
card_version: v0.1
card_type: table_card
domain:
  id: A
  name: 租户与组织
business_meaning: '组织节点（团队/虚拟组）。

  数据字典将该表归入“租户与组织”域。

  数据字典行数说明为：~1.9k。'
grain: '数据字典说明粒度：一行 = 一个组织节点；只覆盖前 200 个 tenant。

  数据字典说明主键：`node_id`。'
primary_key:
- node_id
natural_key_candidate: []
profile:
  row_count: 1901
  column_count: 5
  source_profile_file: outputs/profiles/table_profiles.json
  source_column_profile_file: outputs/profiles/column_profiles.json
  profile_status: validated
  validation_file: outputs/knowledge/validation/dim_org_structure_validation.md
columns:
  node_id:
    type: varchar
    semantic_type: primary_key
    business_meaning: 节点 ID，形如 `N00001_03`
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 1901
      distinct_rate: 1.0
      min: N00001_01
      max: N00200_13
      sample_values:
      - N00001_01
      - N00001_02
      - N00001_03
      - N00001_04
      - N00001_05
      is_possible_primary_key: true
    notes: []
  parent_node_id:
    type: varchar
    semantic_type: foreign_key
    business_meaning: '上级节点 ID（可为空）

      空值、空字符串或混合类型语义来自数据字典说明，仍需结合实际数据分布复核。'
    references: null
    profile:
      null_rate: 0.1052
      distinct_count: 953
      distinct_rate: 0.5013
      min: N00001_01
      max: N00200_09
      sample_values:
      - N00001_01
      - N00001_02
      - N00001_03
      - N00001_04
      - N00001_05
      is_possible_primary_key: false
    notes: []
  tenant_id:
    type: varchar
    semantic_type: foreign_key
    business_meaning: 所属企业
    references: dim_tenant.tenant_id
    profile:
      null_rate: 0.0
      distinct_count: 200
      distinct_rate: 0.1052
      min: T00001
      max: T00200
      sample_values:
      - T00001
      - T00002
      - T00003
      - T00004
      - T00005
      is_possible_primary_key: false
    notes: []
  node_type:
    type: varchar
    semantic_type: dimension
    business_meaning: 节点类型。枚举：department / team / virtual_group
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 3
      distinct_rate: 0.0016
      min: department
      max: virtual_group
      sample_values:
      - department
      - team
      - virtual_group
      is_possible_primary_key: false
    notes: []
  level:
    type: bigint
    semantic_type: metric
    business_meaning: 层级深度（0~5）
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 6
      distinct_rate: 0.0032
      min: '0'
      max: '5'
      sample_values:
      - '0'
      - '1'
      - '2'
      - '3'
      - '4'
      is_possible_primary_key: false
    notes: []
join_keys: []
derived_join_paths: {}
field_groups:
  id_columns:
  - node_id
  - parent_node_id
  - tenant_id
  time_columns: []
  metric_columns:
  - level
  dimension_columns:
  - node_id
  - parent_node_id
  - tenant_id
  - node_type
  enum_columns:
  - node_type
  json_columns: []
  text_columns: []
recommended_use_cases:
- 基于数据字典理解 dim_org_structure 的表级业务含义与字段语义
- 在运行 validation 脚本前做主键、粒度、时间口径与枚举口径假设梳理
- 按数据字典声明的枚举字段做结构分布分析
preferred_metrics:
  entity_count:
    expression: COUNT(DISTINCT node_id)
    meaning: 实体或记录数量
    applicable_grain: table_grain
    note: '按当前正式卡 grain 与 validation 结果解释统计口径。

      对于非单列主键表，应结合 grain 与 distinct 规则解释记录数。'
  level_sum:
    expression: SUM(level)
    meaning: level 的SUM指标
    applicable_grain: table_grain
    note: 当前为自动草稿，需要人工确认该数值字段是否适合直接聚合。
known_traps:
- trap: 把数据字典声明的主键直接当作已验证事实。
  consequence: 如果当前 DuckDB 数据与字典存在偏差，可能误判粒度或唯一性。
  prevention: 将“`node_id`”只视为数据字典假设，必须以后续 validation 脚本结果为准。
- trap: 将 COUNT(*) 与 COUNT(DISTINCT 实体键) 混用。
  consequence: 在一对多 JOIN 或非唯一粒度表中，可能把记录数误读成实体数。
  prevention: 先依据数据字典粒度说明和 validation 结果确认使用哪个计数口径。
aggregation_notes:
- 数据字典说明该表粒度为：一行 = 一个组织节点；只覆盖前 200 个 tenant。聚合前必须先验证这一粒度在当前数据中是否成立。
- 当前所有聚合说明都仅基于数据字典，不代表已经通过验证。
validation_summary:
  validation_file: outputs/knowledge/validation/dim_org_structure_validation.md
  profile_filled: true
  grain_validated: true
  dq_validated: true
  ready_for_agent_recommendation: true
  key_results:
    row_count: 1901
    column_count: 5
    validation_checks_total: 12
    validation_checks_failed: 0
    validation_checks_warning: 0
    key_findings:
    - dim_org_structure 行数为 1901，字段数为 5。
    - 主键候选当前唯一。
    - tenant_id 当前全部可关联到 dim_tenant.tenant_id。
    - 主要 SQL patterns 已执行。
    warnings: []
  interpretation:
  - validation 脚本已通过，当前卡已升级为正式 table card。
  - 主键、粒度、外键覆盖率与主要 SQL pattern 以当前 validation report 为准。
  - dim_org_structure 行数为 1901，字段数为 5。
  - 主键候选当前唯一。
  - tenant_id 当前全部可关联到 dim_tenant.tenant_id。
  - 主要 SQL patterns 已执行。
validation_checks:
- basic_table_profile
- column_count
- primary_key_uniqueness
- duplicated_primary_key_examples
- main_foreign_key_coverage_checks
- enum_distribution_checks
- time_date_parse_checks
- numeric_range_checks
- json_parse_checks
- main_sql_patterns_execution_checks
sql_patterns:
  enum_distribution: "SELECT\n node_type,\n COUNT(DISTINCT node_id) AS entity_count\nFROM dim_org_structure\nGROUP BY node_type\nORDER BY entity_count DESC, node_type;"
  metric_by_enum: "SELECT\n node_type,\n ROUND(SUM(level), 2) AS total_level\nFROM dim_org_structure\nGROUP BY node_type\nORDER BY total_level DESC, node_type;"
  join_coverage_sample: "SELECT\n COUNT(*) AS row_count\nFROM dim_org_structure base\nLEFT JOIN dim_tenant ref\n ON base.tenant_id = ref.tenant_id;"
prompt_summary: 'dim_org_structure 是 组织节点（团队/虚拟组）。

  数据字典声明 grain 为：一行 = 一个组织节点；只覆盖前 200 个 tenant。

  数据字典主键说明为：`node_id`。'
manual_review:
  reviewer: null
  review_status: validated
  review_notes:
  - 当前卡已结合 validation report 完成正式化整理。
  - 主键、自然键、JOIN 覆盖率与字段口径以当前 validation report 和正式卡内容为准。
  - 当前卡已结合 validation report 升级为正式版本。
  checklist:
  - confirmed: true
    item: 数据字典声明主键为：`node_id`，已结合当前数据完成唯一性核对。
  - confirmed: true
    item: 数据字典声明粒度为：一行 = 一个组织节点；只覆盖前 200 个 tenant，已结合当前数据完成 grain 核对。
  - confirmed: true
    item: 数据字典说明行数约为：~1.9k，已与当前 profile 和 DuckDB 数据核对。
  - confirmed: true
    item: dim_org_structure 行数为 1901，字段数为 5。
  - confirmed: true
    item: 主键候选当前唯一。
  - confirmed: true
    item: tenant_id 当前全部可关联到 dim_tenant.tenant_id。
  - confirmed: true
    item: 主要 SQL patterns 已执行。
  required: false
  reviewed: true
  review_source: outputs/knowledge/validation/dim_org_structure_validation.md
status:
  authoring_status: validated
  profile_filled: true
  grain_validated: true
  dq_validated: true
  ready_for_agent: true
