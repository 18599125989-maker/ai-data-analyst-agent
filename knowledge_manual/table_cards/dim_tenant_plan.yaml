table_name: dim_tenant_plan
card_version: v0.1
card_type: table_card
domain:
  id: A
  name: 租户与组织
business_meaning: '租户套餐历史（SCD2）。

  数据字典将该表归入“租户与组织”域。

  数据字典行数说明为：~2.3k。'
grain: '数据字典说明粒度：一行 = 一个租户在某个时间区间使用的套餐。

  数据字典说明主键：`tenant_id` + `effective_from`。'
primary_key:
- tenant_id
- effective_from
natural_key_candidate:
- tenant_id
- effective_from
natural_key_status:
  is_unique: true
  note: '该候选自然键用于表达业务粒度或替代性唯一键，使用前应结合本卡 validation 结果理解。

    当前卡已升级为正式版本，唯一性与使用规则以本卡和 validation report 为准。'
profile:
  row_count: 2320
  column_count: 5
  source_profile_file: outputs/profiles/table_profiles.json
  source_column_profile_file: outputs/profiles/column_profiles.json
  profile_status: validated
  validation_file: outputs/knowledge/validation/dim_tenant_plan_validation.md
columns:
  tenant_id:
    type: varchar
    semantic_type: composite_key_part
    business_meaning: '关联 `dim_tenant.tenant_id`

      以上字段说明来自数据字典，关联关系仍需验证脚本确认覆盖率与方向。'
    references: dim_tenant.tenant_id
    profile:
      null_rate: 0.0
      distinct_count: 500
      distinct_rate: 0.2155
      min: T00001
      max: T00500
      sample_values:
      - T00001
      - T00002
      - T00003
      - T00004
      - T00005
      is_possible_primary_key: false
    notes: []
  plan_name:
    type: varchar
    semantic_type: dimension
    business_meaning: 套餐展示名：Free / Starter / Professional / Business / Enterprise
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 5
      distinct_rate: 0.0022
      min: Business
      max: Starter
      sample_values:
      - Business
      - Enterprise
      - Free
      - Professional
      - Starter
      is_possible_primary_key: false
    notes: []
  plan_tier:
    type: varchar
    semantic_type: dimension
    business_meaning: 套餐等级（小写短语）：free / starter / pro / business / enterprise
    references: dim_plan.plan_tier
    profile:
      null_rate: 0.0
      distinct_count: 5
      distinct_rate: 0.0022
      min: business
      max: starter
      sample_values:
      - business
      - enterprise
      - free
      - pro
      - starter
      is_possible_primary_key: false
    notes: []
  effective_from:
    type: date
    semantic_type: composite_key_part
    business_meaning: 该套餐生效开始日
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 661
      distinct_rate: 0.2849
      min: '2024-04-01'
      max: '2026-03-30'
      sample_values:
      - '2024-04-01'
      - '2024-04-02'
      - '2024-04-04'
      - '2024-04-05'
      - '2024-04-09'
      is_possible_primary_key: false
    notes: []
  effective_to:
    type: date
    semantic_type: event_time
    business_meaning: '该套餐失效日；**空字符串表示当前仍有效**

      空值、空字符串或混合类型语义来自数据字典说明，仍需结合实际数据分布复核。'
    references: null
    profile:
      null_rate: 0.2134
      distinct_count: 545
      distinct_rate: 0.2349
      min: '2024-06-18'
      max: '2026-03-31'
      sample_values:
      - '2024-06-18'
      - '2024-06-21'
      - '2024-06-27'
      - '2024-07-03'
      - '2024-07-04'
      is_possible_primary_key: false
    notes: []
join_keys:
- column: tenant_id
  references: dim_tenant.tenant_id
  relationship: many_to_one
  join_condition: dim_tenant_plan.tenant_id = dim_tenant.tenant_id
  purpose: '数据字典说明中的主关联路径。

    当前正式卡可作为推荐 JOIN 路径使用。'
  risk_level: low
  validation_status: passed
  note: '字段说明明确 tenant_id 关联 dim_tenant.tenant_id。

    当前正式卡已通过对应 validation 检查，使用时仍需遵守本卡 grain 与 known_traps 规则。'
- column: plan_tier
  references: dim_plan.plan_tier
  relationship: many_to_one
  join_condition: dim_tenant_plan.plan_tier = dim_plan.plan_tier
  purpose: '数据字典说明中的主关联路径。

    当前正式卡可作为推荐 JOIN 路径使用。'
  risk_level: low
  validation_status: passed
  note: '字段说明写明 plan_tier 为套餐等级，可与 dim_plan 的套餐等级对照。

    当前正式卡已通过对应 validation 检查，使用时仍需遵守本卡 grain 与 known_traps 规则。'
derived_join_paths: {}
field_groups:
  id_columns:
  - tenant_id
  - effective_from
  time_columns:
  - effective_from
  - effective_to
  metric_columns: []
  dimension_columns:
  - tenant_id
  - plan_name
  - plan_tier
  - effective_from
  - effective_to
  enum_columns:
  - plan_name
  - plan_tier
  json_columns: []
  text_columns: []
recommended_use_cases:
- 基于数据字典理解 dim_tenant_plan 的表级业务含义与字段语义
- 在运行 validation 脚本前做主键、粒度、时间口径与枚举口径假设梳理
preferred_metrics:
  entity_count:
    expression: COUNT(*)
    meaning: 实体或记录数量
    applicable_grain: table_grain
    note: '按当前正式卡 grain 与 validation 结果解释统计口径。

      对于非单列主键表，应结合 grain 与 distinct 规则解释记录数。'
known_traps:
- trap: 把数据字典声明的主键直接当作已验证事实。
  consequence: 如果当前 DuckDB 数据与字典存在偏差，可能误判粒度或唯一性。
  prevention: 将“`tenant_id` + `effective_from`”只视为数据字典假设，必须以后续 validation 脚本结果为准。
- trap: 忽略数据字典中对空字符串、N/A 或混合类型的特别说明。
  consequence: 可能把“当前仍有效”“未完成调研”或“缺失值”误当成普通值。
  prevention: 先按数据字典说明理解异常取值语义，再用验证脚本检查实际分布。
- trap: 将 COUNT(*) 与 COUNT(DISTINCT 实体键) 混用。
  consequence: 在一对多 JOIN 或非唯一粒度表中，可能把记录数误读成实体数。
  prevention: 先依据数据字典粒度说明和 validation 结果确认使用哪个计数口径。
aggregation_notes:
- 数据字典说明该表粒度为：一行 = 一个租户在某个时间区间使用的套餐。聚合前必须先验证这一粒度在当前数据中是否成立。
- 当前所有聚合说明都仅基于数据字典，不代表已经通过验证。
validation_summary:
  validation_file: outputs/knowledge/validation/dim_tenant_plan_validation.md
  profile_filled: true
  grain_validated: true
  dq_validated: true
  ready_for_agent_recommendation: true
  key_results:
    row_count: 2320
    column_count: 5
    validation_checks_total: 17
    validation_checks_failed: 0
    validation_checks_warning: 0
    key_findings:
    - dim_tenant_plan 行数为 2320，字段数为 5。
    - 主键候选当前唯一。
    - tenant_id 当前全部可关联到 dim_tenant.tenant_id。
    - plan_tier 当前全部可关联到 dim_plan.plan_tier。
    - 主要 SQL patterns 已执行。
    warnings: []
  interpretation:
  - validation 脚本已通过，当前卡已升级为正式 table card。
  - 主键、粒度、外键覆盖率与主要 SQL pattern 以当前 validation report 为准。
  - dim_tenant_plan 行数为 2320，字段数为 5。
  - 主键候选当前唯一。
  - tenant_id 当前全部可关联到 dim_tenant.tenant_id。
  - plan_tier 当前全部可关联到 dim_plan.plan_tier。
  - 主要 SQL patterns 已执行。
validation_checks:
- basic_table_profile
- column_count
- primary_key_uniqueness
- duplicated_primary_key_examples
- main_foreign_key_coverage_checks
- enum_distribution_checks
- time_date_parse_checks
- numeric_range_checks
- json_parse_checks
- main_sql_patterns_execution_checks
sql_patterns:
  time_distribution: "SELECT\n effective_from AS time_key,\n COUNT(*) AS row_count\nFROM dim_tenant_plan\nGROUP BY effective_from\nORDER BY effective_from;"
  enum_distribution: "SELECT\n plan_name,\n COUNT(DISTINCT tenant_id) AS entity_count\nFROM dim_tenant_plan\nGROUP BY plan_name\nORDER BY entity_count DESC, plan_name;"
  join_coverage_sample: "SELECT\n COUNT(*) AS row_count\nFROM dim_tenant_plan base\nLEFT JOIN dim_tenant ref\n ON base.tenant_id = ref.tenant_id;"
prompt_summary: 'dim_tenant_plan 是 租户套餐历史（SCD2）。

  数据字典声明 grain 为：一行 = 一个租户在某个时间区间使用的套餐。

  数据字典主键说明为：`tenant_id` + `effective_from`。

  常用 JOIN 假设包括：tenant_id -> dim_tenant.tenant_id；plan_tier -> dim_plan.plan_tier。'
manual_review:
  reviewer: null
  review_status: validated
  review_notes:
  - 当前卡已结合 validation report 完成正式化整理。
  - 主键、自然键、JOIN 覆盖率与字段口径以当前 validation report 和正式卡内容为准。
  - 当前卡已结合 validation report 升级为正式版本。
  checklist:
  - confirmed: true
    item: 数据字典声明主键为：`tenant_id` + `effective_from`，已结合当前数据完成唯一性核对。
  - confirmed: true
    item: 数据字典声明粒度为：一行 = 一个租户在某个时间区间使用的套餐，已结合当前数据完成 grain 核对。
  - confirmed: true
    item: 数据字典说明行数约为：~2.3k，已与当前 profile 和 DuckDB 数据核对。
  - confirmed: true
    item: 数据字典给出的 JOIN 假设：dim_tenant_plan.tenant_id -> dim_tenant.tenant_id，已结合当前数据完成覆盖率与口径核对。
  - confirmed: true
    item: 数据字典给出的 JOIN 假设：dim_tenant_plan.plan_tier -> dim_plan.plan_tier，已结合当前数据完成覆盖率与口径核对。
  - confirmed: true
    item: dim_tenant_plan 行数为 2320，字段数为 5。
  - confirmed: true
    item: 主键候选当前唯一。
  - confirmed: true
    item: tenant_id 当前全部可关联到 dim_tenant.tenant_id。
  - confirmed: true
    item: plan_tier 当前全部可关联到 dim_plan.plan_tier。
  - confirmed: true
    item: 主要 SQL patterns 已执行。
  required: false
  reviewed: true
  review_source: outputs/knowledge/validation/dim_tenant_plan_validation.md
status:
  authoring_status: validated
  profile_filled: true
  grain_validated: true
  dq_validated: true
  ready_for_agent: true
