table_name: dim_ticket_category
card_version: v0.1
card_type: table_card
domain:
  id: F
  name: 客服工单
business_meaning: '工单分类。

  数据字典将该表归入“客服工单”域。

  数据字典行数说明为：12，**含两级层级**（通过 `parent_category_id` 构建）。'
grain: 数据字典说明主键：`category_id`。
primary_key:
- category_id
natural_key_candidate: []
profile:
  row_count: 12
  column_count: 4
  source_profile_file: outputs/profiles/table_profiles.json
  source_column_profile_file: outputs/profiles/column_profiles.json
  profile_status: validated
  validation_file: outputs/knowledge/validation/dim_ticket_category_validation.md
columns:
  category_id:
    type: varchar
    semantic_type: primary_key
    business_meaning: 分类 key，如 `account` / `account_login`
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 12
      distinct_rate: 1.0
      min: account
      max: security
      sample_values:
      - account
      - account_login
      - account_permission
      - billing
      - billing_invoice
      is_possible_primary_key: true
    notes: []
  category_name:
    type: varchar
    semantic_type: dimension
    business_meaning: 展示名
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 12
      distinct_rate: 1.0
      min: Account Issues
      max: Security Concerns
      sample_values:
      - Account Issues
      - App Crashes
      - Billing & Payment
      - Bug Report
      - Data Loss
      is_possible_primary_key: true
    notes: []
  parent_category_id:
    type: varchar
    semantic_type: foreign_key
    business_meaning: '上级分类（可为空，表示顶级）

      空值、空字符串或混合类型语义来自数据字典说明，仍需结合实际数据分布复核。'
    references: null
    profile:
      null_rate: 0.5
      distinct_count: 3
      distinct_rate: 0.25
      min: account
      max: bug
      sample_values:
      - account
      - billing
      - bug
      is_possible_primary_key: false
    notes: []
  sla_hours:
    type: bigint
    semantic_type: metric
    business_meaning: SLA 小时数
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 6
      distinct_rate: 0.5
      min: '4'
      max: '72'
      sample_values:
      - '4'
      - '8'
      - '12'
      - '24'
      - '48'
      is_possible_primary_key: false
    notes: []
join_keys:
- column: parent_category_id
  references: dim_ticket_category.category_id
  relationship: many_to_one
  join_condition: dim_ticket_category.parent_category_id = dim_ticket_category.category_id
  purpose: '数据字典说明中的主关联路径。

    当前正式卡可作为推荐 JOIN 路径使用。'
  risk_level: low
  validation_status: passed
  note: '字段说明写明 parent_category_id 表示上级分类。

    当前正式卡已通过对应 validation 检查，使用时仍需遵守本卡 grain 与 known_traps 规则。'
derived_join_paths: {}
field_groups:
  id_columns:
  - category_id
  - parent_category_id
  time_columns: []
  metric_columns:
  - sla_hours
  dimension_columns:
  - category_id
  - category_name
  - parent_category_id
  enum_columns:
  - category_id
  - category_name
  - parent_category_id
  json_columns: []
  text_columns: []
recommended_use_cases:
- 基于数据字典理解 dim_ticket_category 的表级业务含义与字段语义
- 在运行 validation 脚本前做主键、粒度、时间口径与枚举口径假设梳理
preferred_metrics:
  entity_count:
    expression: COUNT(DISTINCT category_id)
    meaning: 实体或记录数量
    applicable_grain: table_grain
    note: '按当前正式卡 grain 与 validation 结果解释统计口径。

      对于非单列主键表，应结合 grain 与 distinct 规则解释记录数。'
  sla_hours_sum:
    expression: SUM(sla_hours)
    meaning: sla_hours 的SUM指标
    applicable_grain: table_grain
    note: 当前为自动草稿，需要人工确认该数值字段是否适合直接聚合。
known_traps:
- trap: 把数据字典声明的主键直接当作已验证事实。
  consequence: 如果当前 DuckDB 数据与字典存在偏差，可能误判粒度或唯一性。
  prevention: 将“`category_id`”只视为数据字典假设，必须以后续 validation 脚本结果为准。
- trap: 将 COUNT(*) 与 COUNT(DISTINCT 实体键) 混用。
  consequence: 在一对多 JOIN 或非唯一粒度表中，可能把记录数误读成实体数。
  prevention: 先依据数据字典粒度说明和 validation 结果确认使用哪个计数口径。
aggregation_notes:
- 当前所有聚合说明都仅基于数据字典，不代表已经通过验证。
validation_summary:
  validation_file: outputs/knowledge/validation/dim_ticket_category_validation.md
  profile_filled: true
  grain_validated: true
  dq_validated: true
  ready_for_agent_recommendation: true
  key_results:
    row_count: 12
    column_count: 4
    validation_checks_total: 13
    validation_checks_failed: 0
    validation_checks_warning: 0
    key_findings:
    - dim_ticket_category 行数为 12，字段数为 4。
    - 主键候选当前唯一。
    - 主要 SQL patterns 已执行。
    warnings: []
  interpretation:
  - validation 脚本已通过，当前卡已升级为正式 table card。
  - 主键、粒度、外键覆盖率与主要 SQL pattern 以当前 validation report 为准。
  - dim_ticket_category 行数为 12，字段数为 4。
  - 主键候选当前唯一。
  - 主要 SQL patterns 已执行。
validation_checks:
- basic_table_profile
- column_count
- primary_key_uniqueness
- duplicated_primary_key_examples
- main_foreign_key_coverage_checks
- enum_distribution_checks
- time_date_parse_checks
- numeric_range_checks
- json_parse_checks
- main_sql_patterns_execution_checks
sql_patterns:
  enum_distribution: "SELECT\n category_id,\n COUNT(DISTINCT category_id) AS entity_count\nFROM dim_ticket_category\nGROUP BY category_id\nORDER BY entity_count DESC, category_id;"
  metric_by_enum: "SELECT\n category_id,\n ROUND(SUM(sla_hours), 2) AS total_sla_hours\nFROM dim_ticket_category\nGROUP BY category_id\nORDER BY total_sla_hours DESC, category_id;"
prompt_summary: 'dim_ticket_category 是 工单分类。

  数据字典声明 grain 为：待补充。

  数据字典主键说明为：`category_id`。

  常用 JOIN 假设包括：parent_category_id -> dim_ticket_category.category_id。'
manual_review:
  reviewer: null
  review_status: validated
  review_notes:
  - 当前卡已结合 validation report 完成正式化整理。
  - 主键、自然键、JOIN 覆盖率与字段口径以当前 validation report 和正式卡内容为准。
  - 当前卡已结合 validation report 升级为正式版本。
  checklist:
  - confirmed: true
    item: 数据字典声明主键为：`category_id`，已结合当前数据完成唯一性核对。
  - confirmed: true
    item: 数据字典说明行数约为：12，**含两级层级**（通过 `parent_category_id` 构建），已与当前 profile 和 DuckDB 数据核对。
  - confirmed: true
    item: 数据字典给出的 JOIN 假设：dim_ticket_category.parent_category_id -> dim_ticket_category.category_id，已结合当前数据完成覆盖率与口径核对。
  - confirmed: true
    item: dim_ticket_category 行数为 12，字段数为 4。
  - confirmed: true
    item: 主键候选当前唯一。
  - confirmed: true
    item: 主要 SQL patterns 已执行。
  required: false
  reviewed: true
  review_source: outputs/knowledge/validation/dim_ticket_category_validation.md
status:
  authoring_status: validated
  profile_filled: true
  grain_validated: true
  dq_validated: true
  ready_for_agent: true
