table_name: dim_ticket_category
card_version: v0.1
card_type: table_card
domain:
  id: F
  name: 客服工单
business_meaning: >-
  工单分类。

  数据字典将该表归入“客服工单”域。

  数据字典行数说明为：12，**含两级层级**（通过 `parent_category_id` 构建）。

  当前 card 仍为 draft，以上内容属于数据字典说明与待验证假设，不能视为验证通过结论。
grain: >-
  数据字典说明主键：`category_id`。

  当前 draft 只能把这些内容视为待验证假设；是否在当前 DuckDB 数据中成立，仍以后续 validation 脚本和人工复核为准。
primary_key:
- category_id
natural_key_candidate: []
profile:
  row_count: 12
  column_count: 4
  source_profile_file: outputs/profiles/table_profiles.json
  source_column_profile_file: outputs/profiles/column_profiles.json
  profile_status: auto_filled_pending_validation
columns:
  category_id:
    type: varchar
    semantic_type: primary_key
    business_meaning: >-
      分类 key，如 `account` / `account_login`

      数据字典说明已补充，仍需人工复核。
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 12
      distinct_rate: 1.0
      min: account
      max: security
      sample_values:
      - account
      - account_login
      - account_permission
      - billing
      - billing_invoice
      is_possible_primary_key: true
    notes:
    - 当前字段说明主要来自 DATA_DICTIONARY.md 与自动 profile，尚未人工验证。
    - 如需依赖该字段做 JOIN、聚合或口径判断，请先运行对应 validate 脚本并人工复核。
  category_name:
    type: varchar
    semantic_type: dimension
    business_meaning: >-
      展示名

      数据字典说明已补充，仍需人工复核。
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 12
      distinct_rate: 1.0
      min: Account Issues
      max: Security Concerns
      sample_values:
      - Account Issues
      - App Crashes
      - Billing & Payment
      - Bug Report
      - Data Loss
      is_possible_primary_key: true
    notes:
    - 当前字段说明主要来自 DATA_DICTIONARY.md 与自动 profile，尚未人工验证。
    - 如需依赖该字段做 JOIN、聚合或口径判断，请先运行对应 validate 脚本并人工复核。
  parent_category_id:
    type: varchar
    semantic_type: foreign_key
    business_meaning: >-
      上级分类（可为空，表示顶级）

      空值、空字符串或混合类型语义来自数据字典说明，仍需结合实际数据分布复核。
    references: null
    profile:
      null_rate: 0.5
      distinct_count: 3
      distinct_rate: 0.25
      min: account
      max: bug
      sample_values:
      - account
      - billing
      - bug
      is_possible_primary_key: false
    notes:
    - 当前字段说明主要来自 DATA_DICTIONARY.md 与自动 profile，尚未人工验证。
    - 如需依赖该字段做 JOIN、聚合或口径判断，请先运行对应 validate 脚本并人工复核。
  sla_hours:
    type: bigint
    semantic_type: metric
    business_meaning: >-
      SLA 小时数

      数据字典说明已补充，仍需人工复核。
    references: null
    profile:
      null_rate: 0.0
      distinct_count: 6
      distinct_rate: 0.5
      min: '4'
      max: '72'
      sample_values:
      - '4'
      - '8'
      - '12'
      - '24'
      - '48'
      is_possible_primary_key: false
    notes:
    - 当前字段说明主要来自 DATA_DICTIONARY.md 与自动 profile，尚未人工验证。
    - 如需依赖该字段做 JOIN、聚合或口径判断，请先运行对应 validate 脚本并人工复核。
join_keys:
- column: parent_category_id
  references: dim_ticket_category.category_id
  relationship: pending_validation
  join_condition: dim_ticket_category.parent_category_id = dim_ticket_category.category_id
  purpose: >-
    数据字典说明中的主关联路径。

    当前仅作为待验证 JOIN 假设使用。
  risk_level: pending_validation
  validation_status: pending_validation
  note: >-
    字段说明写明 parent_category_id 表示上级分类。

    当前不得视为验证通过；覆盖率、方向、大小写/编码兼容性和聚合风险仍需 validation 脚本与人工复核。
derived_join_paths: {}
field_groups:
  id_columns:
  - category_id
  - parent_category_id
  time_columns: []
  metric_columns:
  - sla_hours
  dimension_columns:
  - category_id
  - category_name
  - parent_category_id
  enum_columns:
  - category_id
  - category_name
  - parent_category_id
  json_columns: []
  text_columns: []
recommended_use_cases:
- 基于数据字典理解 dim_ticket_category 的表级业务含义与字段语义
- 在运行 validation 脚本前做主键、粒度、时间口径与枚举口径假设梳理
preferred_metrics:
  entity_count:
    expression: COUNT(DISTINCT category_id)
    meaning: 实体或记录数量
    applicable_grain: 待验证
    note: >-
      当前为自动草稿，需要先验证主键或业务粒度后再确认统计口径。

      对于复合键表，COUNT(*) 仅在 grain 成立时才可解释为记录数。
  sla_hours_sum:
    expression: SUM(sla_hours)
    meaning: sla_hours 的SUM指标
    applicable_grain: 待验证
    note: 当前为自动草稿，需要人工确认该数值字段是否适合直接聚合。
known_traps:
- trap: >-
    把数据字典声明的主键直接当作已验证事实。

    当前仍是 draft。
  consequence: 如果当前 DuckDB 数据与字典存在偏差，可能误判粒度或唯一性。
  prevention: 将“`category_id`”只视为数据字典假设，必须以后续 validation 脚本结果为准。
- trap: >-
    将 COUNT(*) 与 COUNT(DISTINCT 实体键) 混用。

    当前仍是 draft。
  consequence: 在一对多 JOIN 或非唯一粒度表中，可能把记录数误读成实体数。
  prevention: 先依据数据字典粒度说明和 validation 结果确认使用哪个计数口径。
aggregation_notes:
- 当前所有聚合说明都仅基于数据字典，不代表已经通过验证。
validation_summary:
  validation_file: null
  profile_filled: true
  grain_validated: false
  dq_validated: false
  ready_for_agent_recommendation: false
  key_results: {}
  interpretation:
  - 当前为待验证 draft，尚未经过人工确认。
  - 主键、自然键、JOIN 覆盖率、指标口径和 known_traps 均需要验证脚本和人工复核。
validation_checks:
- basic_table_profile
- column_count
- primary_key_uniqueness
- duplicated_primary_key_examples
- main_foreign_key_coverage_checks
- enum_distribution_checks
- time_date_parse_checks
- numeric_range_checks
- json_parse_checks
- main_sql_patterns_execution_checks
sql_patterns:
  enum_distribution: >-
    SELECT
        category_id,
        COUNT(DISTINCT category_id) AS entity_count
    FROM dim_ticket_category

    GROUP BY category_id

    ORDER BY entity_count DESC, category_id;
  metric_by_enum: >-
    SELECT
        category_id,
        ROUND(SUM(sla_hours), 2) AS total_sla_hours
    FROM dim_ticket_category

    GROUP BY category_id

    ORDER BY total_sla_hours DESC, category_id;
prompt_summary: >-
  dim_ticket_category 是 工单分类。

  数据字典声明 grain 为：待补充。

  数据字典主键说明为：`category_id`。

  常用 JOIN 假设包括：parent_category_id -> dim_ticket_category.category_id。

  当前 card 仍是 draft，数据字典中的主键、JOIN、口径和特殊说明都只能视为待验证假设。
manual_review:
  reviewer: null
  review_status: pending_validation
  review_notes:
  - 当前为批量生成的 draft，需要结合 validation report 逐张人工复查。
  - 主键、自然键、JOIN 覆盖率、字段口径和 known_traps 都不能直接视为最终结论。
  checklist:
  - confirmed: false
    item: 数据字典声明主键为：`category_id`。需要核对当前数据是否满足唯一性。
  - confirmed: false
    item: 数据字典说明行数约为：12，**含两级层级**（通过 `parent_category_id` 构建）。需要与当前 profile 和 DuckDB 数据核对。
  - confirmed: false
    item: 数据字典给出的 JOIN 假设：dim_ticket_category.parent_category_id -> dim_ticket_category.category_id。需要验证覆盖率与口径风险。
status:
  authoring_status: manual_draft
  profile_filled: true
  grain_validated: false
  dq_validated: false
  ready_for_agent: false
